你是事件补缺助手。规则扫描发现:在下面这个场景里,人物「{TARGET}」出现了,但当前已识别的事件中没有涉及 ta。请只针对这一点,判断是否确实漏了 ta 参与的事件。

【已识别的本场景事件】
{KNOWN_EVENTS}

【人物候选】(participants 只能从这里选,纯数字 id)
{CHAR_CAND}
【物品候选】(items 只能从这里选,纯数字 id)
{ITEM_CAND}

【任务】只补「{TARGET}」(id={TARGET_ID})真正参与、但上面未列出的事件。如果 ta 在本场景只是被提及、并无实际参与的动作,就返回空数组(说明不算漏)。不要重复已识别的事件,不要臆造。

【每个补充事件字段】desc / participants(纯数字,必含{TARGET_ID}) / items(纯数字) / anchor_text(一句原文原话,逐字)

【输出格式】只输出 JSON。ASCII半角。结构: {"missing_events":[{"desc":"","participants":[],"items":[],"anchor_text":""}]}

【本场景原文】
{SCENE_TEXT}
