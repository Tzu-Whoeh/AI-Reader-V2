# research/org_extraction · 09_org 抽取 prompt + 后处理验证发现

**状态:research,未进主管线。** 在 wangcai/ollama 35b 上对《潜伏》真实章节验证。

## 方法
35b,temp0.12/num_ctx49152/format=json/think=false;ch08/ch05/ch11 各跑 3 次;
锚点校验(NFKC 归一后 mentions/anchor_text 须命中原文)+ org-set 3 跑一致性。
抽取后挂确定性后处理 `org_postproc.clean_organizations`。

## 后处理修复(确定性,不堆 prompt — 守 §8)
1. **NFKC 归一锚点**:根治"７６号"vs"76号"全/半角失配。
2. **泛指停用**:保守清单排"重庆方面/共方/我方/敌方…"等阵营泛称(不排"总部/政府"等可能具名的)。
3. **自造防护**:name 在原文(NFKC)找不到 → 整条删(如模型造的"汪主席政府")。
4. **悬空清理**:成员 org_id 必指向保留组织;explicit 锚点不中 → 降级 inferred 交人工;关系两端须在保留集。

## 结果(诚实)
| 指标 | 后处理前 | 后处理后 |
|---|---|---|
| 锚点失败(9 跑总) | 1-3/跑 | **0** ✓ |
| 硬泛指过抽(重庆方面/共方) | 常现 | **已清除** ✓ |
| 自造名(汪主席政府) | 偶现 | **已删** ✓ |
| 核心组织 | 稳 | 稳(76号/军统/满洲国 // 地下党/区委/行动部/群工部 // 大东亚日报) |
| org-set 3 跑完全一致 | 否 | **仍否** |

## 结论:后处理把"噪音/错误"清干净了,**剩余不一致是语义粒度的固有变动**,非 bug:
- ch05:核心 5 稳;仅"国民党"边缘闪烁(原文确实弱出现)。
- ch08:核心 3 稳;"汪主席/金大"边缘闪烁。
- ch11:核心稳;模型在"76号"与"汪伪政权/伪满洲国"间换名(同一汪伪阵营的不同有效叫法)。

这与 AGENT.md §5 记录的 abliterated 边缘漂移一致。强求 set 完全相等需过度工程化,不划算。

## 进管线的判断(交人工定)
**支持进**:核心组织稳定且锚点 0 失败;边缘变体/低频本就该由跨章归一(`resolve_global_entities`)+ `ambiguities` 人工复核兜底——这正是现有架构为不确定项设计的出口。即组织维度可先上,边缘噪音由下游既有机制消化。
**保守派**:若要求"逐章 3 跑一致"硬门槛,则未达标,需两段式复核进一步压边缘(成本翻倍,收益边际)。

## 产物(research/org_extraction/,主管线零改动)
`09_org_extraction.txt`(v2)、`org_postproc.py`(确定性后处理)、`run_org_extract.py`(runner)、`FINDINGS.md`。